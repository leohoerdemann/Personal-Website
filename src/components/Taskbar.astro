<script>
  function updateClock() {
  const now = new Date();
  let hours = now.getHours();
  const ampm = hours >= 12 ? 'PM' : 'AM';
  hours = hours % 12 || 12;
  hours = hours.toString().padStart(2, '0');
  const minutes = now.getMinutes().toString().padStart(2, '0');
  document.getElementById('taskbar-clock').textContent = `${hours}:${minutes} ${ampm}`;
}

// Update clock every minute
setInterval(updateClock, 1000);
updateClock();

</script>



<div class="taskbar">
  <div class="taskbar-icons">
    <div class="icon">
      <img src="/Icons/world.png" alt="Start">
    </div>
    <div class="icon">
      <img src="/Icons/user.png" alt="App">
    </div>
    <div class="icon">
      <img src="/Icons/document.png" alt="App">
    </div>
    <!-- Add more icons as needed -->
  </div>
  <div class="taskbar-clock" id="taskbar-clock">
  </div>
</div>


<style>
  :root {
  --taskbar-height: 45px; /* Adjust as needed to fit your icons */
  --taskbar-border-color: rgba(0, 0, 0, 0.7); /* Matching border from title bar */
  --taskbar-background-color: rgba(69, 128, 196, 0.25); /* Blue tint from the title bar glass effect */
  --taskbar-glass-stripes: linear-gradient(
      135deg,
      rgba(255, 255, 255, 0.1) 70px,
      transparent 100px
    ),
    linear-gradient(225deg, rgba(255, 255, 255, 0.1) 70px, transparent 100px);
  --taskbar-blur: blur(7px); /* Same blur as in the title bar */
  --taskbar-icon-size: 40px; /* Icon size to fill the taskbar */
  --taskbar-icon-spacing: 12px; /* Spacing for icons */
}

.taskbar {
  position: fixed;
  bottom: 0;
  left: 0;
  width: 100%;
  height: var(--taskbar-height);
  background: var(--taskbar-background-color);
  backdrop-filter: var(--taskbar-blur); /* Exact glass blur from title bar */
  border-top: 1px solid var(--taskbar-border-color); /* Top border for clean edge */
  box-shadow: 0 -1px 4px rgba(0, 0, 0, 0.3); /* Shadow to lift the taskbar */
  display: flex;
  justify-content: space-between;
  align-items: center; /* Centering icons and clock */
  padding: 0 8px;
  z-index: 1000;
  background-image: var(--taskbar-glass-stripes), var(--taskbar-background-color);
  background-blend-mode: overlay;
  background-clip: padding-box;
  border-radius: 0px; /* Slightly less rounded corners like the title bar */
}

.taskbar-icons {
  display: flex;
  align-items: center;
}

.taskbar-icons .icon {
  width: var(--taskbar-icon-size);
  height: var(--taskbar-icon-size);
  margin-right: var(--taskbar-icon-spacing);
  display: flex;
  align-items: center;
  justify-content: center;
  cursor: pointer;
  border-radius: 2px; /* Minimal rounding for icons */
  background-color: rgba(255, 255, 255, 0); /* Light glassy background for icons */
  transition: background-color 0.2s ease;
}

.taskbar-icons .icon:hover {
  background-color: rgba(255, 255, 255, 0.4); /* Slight hover effect */
}

.taskbar-icons .icon img {
  width: 100%;
  height: 100%;
  object-fit: contain;
}

.taskbar-clock {
  color: #fff;
  font-size: 14px;
  font-family: "Segoe UI", sans-serif;
  text-shadow: 0 0 3px rgba(0, 0, 0, 0.5);
  padding-right: 15px;
  display: flex;
  align-items: center; /* Centering clock */
}

/* Exact glass effect from the title bar */
.taskbar::before {
  content: "";
  position: absolute;
  z-index: -1;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background: linear-gradient(transparent 20%, #ffffff16 80%, transparent 100%),
    var(--taskbar-background-color);
  box-shadow: inset 0 0 0 1px #fffd; /* Inset shadow like the title bar */
  border-radius: 0px; /* Same as the taskbar */
  opacity: 0.6; /* Glass opacity from title bar */
  pointer-events: none;
}

.taskbar::after {
  content: "";
  position: absolute;
  z-index: -10;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  border-radius: 4px;
  border: none;
  backdrop-filter: blur(4px); /* Light blur behind the glass */
  background: var(--taskbar-glass-stripes);
  opacity: 0.3; /* Soft stripe effect */
}

</style>